# Production environment for VPS deployment
# Copy to /opt/pfn-mcp/.env

# Database connection (localhost since DB is on same VPS)
DATABASE_URL=postgresql://postgres:QA4nhPt0Z8Y@localhost:5432/valkyrie

# Connection pool settings
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_QUERY_TIMEOUT=30.0

# Server settings
SERVER_NAME=pfn-mcp
SERVER_VERSION=0.1.0
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Timezone
DISPLAY_TIMEZONE=Asia/Jakarta
